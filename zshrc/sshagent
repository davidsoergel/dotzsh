alias fixssh='source ~/.zsh/updatessh'
alias sshinfo='source ~/.zsh/sshinfo'

if [[ $OSX != 1 ]] && [ -v SSH_AGENT_ENABLED ]; then
    source ~/.sshkeys.local
fi
echo
echo ------------------ SSH Keys -------------------
ssh-add -l
echo
echo -----------------------------------------------
